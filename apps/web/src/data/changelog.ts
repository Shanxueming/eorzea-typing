/**
 * 更新说明。
 *
 * ★ 加新版本时:往 CHANGELOG 顶部插一条,并把 LATEST_VERSION 改成它的 version。
 *   玩家第一次看到某个版本会自动弹一次,点「我知道了」之后记进 localStorage,
 *   下次不再自动弹;想再看从主菜单的「更新说明」进。
 */

export interface ChangelogEntry {
  version: string;
  date: string;
  /** 一句话概括,显示在标题下面 */
  summary: string;
  sections: { title: string; items: string[] }[];
}

export const CHANGELOG: ChangelogEntry[] = [
  {
    version: '2026.08.05',
    date: '2026-08-05',
    summary: '新增无限模式、角色技能、Boss 特殊机制,四档难度彻底拉开差距',
    sections: [
      {
        title: '新玩法',
        items: [
          '**无限模式**:没有狂暴倒计时,泰坦打倒一只立刻刷下一只、血量逐只加厚,击杀回血,撑到倒下为止。比的是能扛到第几只。',
          '**组合输入**(现在是默认):打错不再立刻判负 —— 输入框变红,退格改到变绿就算过。想要以前那种「错一个字立刻结算」的手感,选逐字输入。',
          '**出战角色**:黑皮猫娘「原初的解放」跳过当前词且连击不断(不造成伤害);灰皮猫娘「浴血」接下来三个词奖惩都 ×1.5、但输入时限少 25%。冷却 90 秒,一局大约能开两次。',
        ],
      },
      {
        title: 'Boss 机制',
        items: [
          '**三连桶**:泰坦血量到 67% 时出现。你随机站在跑道最左或最右,打对应方向的词左右挪动,躲到中间石头后面才算安全。打错方向只是原地不动,不会倒退。',
          '**三穿一**:血量到 33% 时出现。三个词标着 1/2/3,必须按顺序打完;打错顺序只作废那一个,计时不停。',
          '**泰坦之怒**改成保底机制:越久没出现概率越高,触发后重置;刚打完的三个词内不会连着再来一次(地狱难度除外)。',
          '联机下三连桶和三穿一**两人位置与判定各自独立**,队友躲到哪一格你能看见。',
        ],
      },
      {
        title: '难度重做',
        items: [
          '以前普通和困难只差一个时限倍数,实战几乎感觉不到。现在四档在**词库字数、限时、读条窗口、扣血、Boss 血量**上全都不同。',
          '普通词限时:简单 25 秒 / 普通 16 秒 / 困难 12 秒 / 地狱 9 秒。',
          '打错扣血:简单 3 / 普通 5 / 困难 10 / 地狱 20。',
          '词库字数:简单 1~3 字、普通 1~6 字、困难与地狱 3~7 字。',
          'Boss 血量:困难 +30%、地狱 +100%。',
          '**简单难度不再触发任何 Boss 机制**,纯粹练手用。',
          '地狱难度强制逐字输入 —— 那一档的规则就是打错一个字立刻判负。',
        ],
      },
      {
        title: '移动端',
        items: [
          '不再劝退移动端。手机上可以正常玩**单人模式**(联机在窄屏下不提供)。',
          '输入框放大到不会触发 iOS 聚焦缩放,按钮都放大到能用拇指点。',
        ],
      },
      {
        title: '修复',
        items: [
          '**修掉一个会让联机彻底玩不下去的问题**:打对词的同时若触发泰坦之怒,客户端与服务端的出词顺序会永久错开一格,之后你打对多少词都不算数、只会被超时一路扣血到团灭,而且不报任何错。',
          '「放弃本次打断」在机制期间点了没反应。',
          '桌面浏览器偶尔会被误判成手机、联机入口消失。',
          '词库某个分类加载失败后,整个页面生命周期内都无法重试。',
          '服务端补上输入校验与频率限制。',
        ],
      },
      {
        title: '关于排行榜',
        items: [
          '单机成绩现在也会跑反作弊检测,结算页会告诉你这一局的可信度。',
          '排行榜还没上线。上线时成绩会由服务端重新核算一遍,本地算出来的可信度不作数。',
        ],
      },
    ],
  },
];

export const LATEST_VERSION = CHANGELOG[0].version;

const STORAGE_KEY = 'eorzea:changelog-seen';

/** 玩家有没有看过最新这版更新说明 */
export function hasSeenLatest(): boolean {
  try {
    return window.localStorage.getItem(STORAGE_KEY) === LATEST_VERSION;
  } catch {
    // 无痕模式读不到 —— 当作没看过,大不了每次都弹一下
    return false;
  }
}

export function markLatestSeen(): void {
  try {
    window.localStorage.setItem(STORAGE_KEY, LATEST_VERSION);
  } catch {
    // 写不进去就算了,不影响玩
  }
}
